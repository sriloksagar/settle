import React from 'react';
import { Home, Users, PlusCircle, Activity, User } from 'lucide-react';

const App = () => {
  // ... (The entire App component code from the previous response)
};

const ExpenseItem = ({ date, icon, description, amount, lent }) => (
  // ... (The ExpenseItem component code from the previous response)
);

const NavItem = ({ icon, label, active }) => (
  // ... (The NavItem component code from the previous response)
);

export default App;
